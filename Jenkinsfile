pipeline {
    agent any

    stages {
        stage('Docker Build'){
            steps {
                sh '''
                export PATH=/sbin:/usr/sbin:/bin:/usr/bin:/usr/local/bin/
                echo 'test docker'
                '''
                
        
            }
        }
    }
}
